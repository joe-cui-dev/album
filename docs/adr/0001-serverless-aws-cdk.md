# Use AWS Serverless Infrastructure Managed by CDK

The album will use an AWS serverless architecture, with TypeScript and Node.js running in event-driven components rather than a permanently running application server. Infrastructure will be defined with AWS CDK so the deployed system is reproducible, reviewable, and avoids manual console drift while keeping idle compute cost near zero.
