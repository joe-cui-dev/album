{
  "name": "@album/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "USER_ALLOWLIST=user-1:user@example.com PHOTOS_BUCKET_NAME=test-bucket METADATA_TABLE_NAME=test-table SESSION_SIGNING_SECRET=test-secret jest --config ./jest.config.cjs",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@album/shared": "0.1.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-sesv2": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "typescript": "^5.7.0"
  }
}
